upstream <%= node['my-nginx']['app'] %> {
  server unix:///var/www/<%= node['my-nginx']['app'] %>/shared/sockets/puma.sock fail_timeout=0;
}

server {
#  listen 80;
  server_name <%= node['my-nginx']['servername'] %>; # change to match your URL
  root <%= node['my-nginx']['default_root'] %>/<%= node['my-nginx']['app'] %>/current/public;

  access_log <%= node['nginx']['log_dir'] %>/<%= node['my-nginx']['app'] %>.access.log;
  error_log <%= node['nginx']['log_dir'] %>/<%= node['my-nginx']['app'] %>.error.log info;

  try_files $uri/index.html $uri @<%= node['my-nginx']['app'] %>;

  location @<%= node['my-nginx']['app'] %> {
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $http_host;
    proxy_redirect off;

    proxy_pass http://<%= node['my-nginx']['app'] %>;
    }
  location ~ ^/(assets)/  {
    gzip_static on; # to serve pre-gzipped version
    expires max;
    add_header Cache-Control public;
    }
# auth_basic "Welcome to <%= node['my-nginx']['app'] %>";
# auth_basic_user_file <%= node['my-nginx']['default_root'] %>/<%= node['my-nginx']['app'] %>/current/.htpasswd;


  error_page 500 502 503 504 /500.html;
  client_max_body_size 4G;
  keepalive_timeout 10;

}

